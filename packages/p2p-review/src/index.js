// @ts-check
// Layer C — four-eyes delegation. For high-value or high-risk cases, sends a
// minimal, end-to-end-encrypted case bundle (normalized fields + fired checks +
// evidence snippets; no raw files) to a second reviewer device over QVAC P2P
// delegated inference, with fallback to local inference so a verdict is always
// reached. See the project README. Implementation lands in Week 2.
export {};
