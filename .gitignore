# Dependencies
node_modules/

# Build / cache
dist/
build/
*.tsbuildinfo

# Environment & secrets
.env
.env.*
!.env.example

# Models & large artifacts (fetched locally, never committed)
*.gguf
models/

# Logs
*.log

# OS / editor
.DS_Store
Thumbs.db

# Private, in-house artifacts — these live OUTSIDE this repo; never commit a stray copy
/PROJECT_CONTEXT.md
/Strategy.md
/specs/
/search/
/.claude/
